using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LogTest : MonoBehaviour
{
    public Logger logger;
    // Start is called before the first frame update
    void Start()
    {
        //logger.Log("this is a test", Logger.LogLevel.Error, gameObject);
    }

    private void Update()
    {
        //logger.Log("this is an update test", Logger.LogLevel.Error, gameObject);
    }
}
